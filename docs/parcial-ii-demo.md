# Parcial II - Pet Radar Demo

Este archivo documenta la evidencia para el video de entrega:

- API levantada con Docker Compose.
- Imagen Docker publicada en GitHub Container Registry mediante GitHub Actions.
- Endpoint `POST /found-pets` ejecutando busqueda por radio con PostGIS.
- Respuesta con `matches_found` y `distance_meters`.
